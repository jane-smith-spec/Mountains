/**
 * raycaster.h — Horizon profile computation via ray-casting.
 *
 * This is the core algorithm: for each compass direction, cast a ray outward
 * from the observer across the DEM, tracking the maximum elevation angle.
 * The result is the horizon profile — the shape of the ridgeline as seen
 * from that position.
 *
 * Implementation will come in Step 5.
 */

#ifndef PEAKLINE_RAYCASTER_H
#define PEAKLINE_RAYCASTER_H

#include "peakline.h"
#include "dem_io.h"

/* Placeholder — full implementation in Step 5. */

#endif /* PEAKLINE_RAYCASTER_H */
