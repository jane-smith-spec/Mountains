/**
 * skyline_match.h — Skyline matching for photo heading detection.
 *
 * When a user imports a photo that lacks compass data, we need to figure out
 * which direction they were facing. This module extracts the mountain
 * silhouette from the photo and matches it against DEM-computed profiles
 * using normalized cross-correlation (NCC).
 *
 * Implementation will come in Step 17.
 */

#ifndef PEAKLINE_SKYLINE_MATCH_H
#define PEAKLINE_SKYLINE_MATCH_H

/* Placeholder — full implementation in Step 17. */

#endif /* PEAKLINE_SKYLINE_MATCH_H */
